<li>
    {{burger_name}}

    {{#if devoured}}
    <button class="btn btn-outline-dark btn-sm" class="change-devoured" data-id="{{id}}"
        data-newdevoured="{{devoured}}">
        DEVOUR!
    </button>
    {{/if}}
</li>